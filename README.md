# manage-orders
This project is a Java Spring Boot application that manages orders. It uses Kafka for messaging and Maven for dependency management. The configuration includes a Kafka topic for order events. The application is structured to support scalable and maintainable order processing.
